include: package:lints/recommended.yaml

linter:
  rules:
    - unawaited_futures
    - prefer_single_quotes
    - require_trailing_commas

analyzer:
  exclude:
    - bin/*.reflectable.dart
    - example/*.reflectable.dart
    - test/*.reflectable.dart
